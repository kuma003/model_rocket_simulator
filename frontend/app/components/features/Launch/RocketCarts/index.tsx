import type { RocketParams } from "../../Rocket/types";

export interface RocketCartsProps {
  rocketParams: RocketParams;
}

const RocketCarts: React.FC<RocketCartsProps> = (rocketParams) => {
  rocketParams;

  return <></>;
};

export default RocketCarts;
